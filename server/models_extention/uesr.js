class User {
    constructor(member, applys, offers) {
        this.memberid = member.id
        this.applys = applys
        this.offers = offers

        // for (apply of applys) {
        //     if (apply.)
        // }
    },
    userid: null
}
